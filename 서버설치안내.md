# 서버 설치 안내 (Supabase)

근무일지를 여러 사람이 함께 쓰려면 Supabase 를 붙인다.
**시설 예약 대장과 같은 프로젝트를 쓰고 표만 새로 만든다** — 그래야 계정이 공유되어
직원이 두 번 가입하지 않는다.

---

## 1단계 — 프로젝트 정하기

이미 시설 예약 대장이 쓰는 프로젝트(`facility-ledger`, 리전 서울)가 있으면 **그것을 쓴다.**
새로 만들 이유가 없다. 무료 플랜 프로젝트를 하나 더 만들면 관리 지점만 둘로 늘어난다.

---

## 2단계 — 표 만들기 ⭐

Supabase 대시보드 → **SQL Editor** → 아래를 통째로 붙여넣고 실행한다.

```sql
-- ═══════════════════════════════════════════════════════════
-- 1) 설정 — 사업장·양식. 담당자만 고치고 자주 바뀌지 않아 한 행에 담는다
-- ═══════════════════════════════════════════════════════════
create table if not exists worklog_config (
  id          text primary key,
  data        jsonb       not null default '{}'::jsonb,
  updated_at  timestamptz not null default now(),
  updated_by  text
);

-- ═══════════════════════════════════════════════════════════
-- 2) 일지 — **한 건이 한 행이다**
--    시설 예약 대장처럼 전체를 한 행에 담으면, 사업장마다 다른 사람이
--    동시에 쓸 때 한 사람이 저장하는 순간 남이 쓴 일지가 통째로 사라진다.
-- ═══════════════════════════════════════════════════════════
create table if not exists worklog (
  id          text primary key,
  site_id     text        not null,
  log_date    date        not null,
  shift       text        not null default '',
  start_at    text        not null default '09:00',
  end_at      text        not null default '18:00',
  draft       boolean     not null default false,
  data        jsonb       not null default '{}'::jsonb,
  author      text,
  updated_at  timestamptz not null default now(),
  updated_by  text
);

create index if not exists worklog_date_idx      on worklog (log_date);
create index if not exists worklog_site_date_idx on worklog (site_id, log_date);
create index if not exists worklog_updated_idx   on worklog (updated_at desc);
```

---

## 3단계 — RLS 켜기 ⭐⭐ (가장 중요)

**이걸 안 켜면 주소만 아는 사람이 근무일지를 통째로 읽고 고칠 수 있다.**
근무일지에는 근무자 이름과 출입 기록이 들어 있으므로, 시설 예약 대장보다 훨씬 중요하다.

```sql
alter table worklog        enable row level security;
alter table worklog_config enable row level security;

-- ── 일지: 로그인한 사람만 읽고 쓴다 ──────────────────────────
--    익명 조회를 열어 두지 않는다. 화면에서 감추는 것과 별개로
--    서버가 막아야 개발자도구로도 못 본다
create policy worklog_select on worklog for select to authenticated using (true);
create policy worklog_insert on worklog for insert to authenticated with check (true);
create policy worklog_update on worklog for update to authenticated using (true) with check (true);
create policy worklog_delete on worklog for delete to authenticated using (true);

-- ── 설정: 누구나 읽고, 로그인한 사람만 쓴다 ──────────────────
--    사업장 이름과 양식은 민감하지 않다. 로그인 화면을 그리려면
--    로그인 전에도 읽을 수 있어야 한다
create policy cfg_select on worklog_config for select using (true);
create policy cfg_insert on worklog_config for insert to authenticated with check (true);
create policy cfg_update on worklog_config for update to authenticated using (true) with check (true);
```

---

## 4단계 — 신규 가입 차단 ⭐⭐

**안 켜면 누구나 계정을 만들어 근무일지를 읽고 고칠 수 있다.**

**Authentication → Sign In / Providers → Email** 에서

- `Allow new users to sign up` **끈다**
- `Confirm email` 은 켠 채로 두어도 되고, 아래 5단계에서 Auto Confirm 으로 계정을 만든다

---

## 5단계 — 계정 만들기

**Authentication → Users → Add user** 에서 한 명씩 만든다.

- `Auto Confirm User` 를 **켠다** (메일 확인 절차를 건너뛴다)
- 근무자마다 계정을 만든다 — 누가 썼는지가 `updated_by` 에 남는다

만든 뒤 화면에서 **⚙ 설정 → 구성원 · 권한 → 담당자 계정** 에 담당자 이메일을 적는다.

> ⚠️ **담당자 목록이 비어 있으면 로그인한 모든 계정이 담당자가 된다.**
> 사업장·양식까지 고칠 수 있게 되므로, 계정을 만들면 이 목록도 함께 채운다.

---

## 6단계 — 주소·키 넣기

`index.html` 위쪽의 두 줄을 고친다.

```js
const SUPABASE_URL = "https://xxxxxxxx.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_...";
```

- **주소는 프로젝트 기본 주소만 넣는다.** 대시보드에서 복사한 `.../rest/v1` 을 그대로 넣으면
  `.../rest/v1/auth/v1/token` 이 되어 로그인이 404 로 실패한다
  (`normalizeUrl()` 이 떼어내지만, 처음부터 바르게 넣는 편이 낫다)
- **publishable key** (`sb_publishable_...`) 를 넣는다. 공개돼도 되는 키다 —
  실제 권한은 위 RLS 가 정한다
- **`sb_secret_` / `service_role` 키는 절대 넣지 않는다.** 저장소에도, 대화창에도 안 된다

---

## 7단계 — 확인

브라우저에서 `index.html` 을 열고 아래를 본다.

- [ ] ⚙ 설정 에 「연결됨 · 조회 전용」 이 뜬다
- [ ] 로그인하면 「연결됨 · 쓰기 가능」 으로 바뀐다
- [ ] 일지를 쓰면 달력에 뜨고, 새로고침해도 남아 있다
- [ ] **다른 브라우저(시크릿 창)에서 로그인하지 않고 열면 근무자 이름이 안 보인다**
- [ ] 개발자도구 → Elements 에서 찾아도 근무자 이름이 소스에 없다
- [ ] 두 창에서 각각 로그인해 서로 다른 일지를 쓰면 **둘 다 남는다** (덮어쓰지 않는다)

마지막 항목이 이 구조를 고른 이유다. 꼭 확인한다.

---

## 다음 단계 — 사업장별 권한 (지금은 안 해도 된다)

사업장이 여럿이 되면 "자기 초소 것만 쓰기" 를 서버가 막게 할 수 있다.
**시설 예약 대장에서는 못 했던 것이다** — 대장은 전체가 한 행이라 열 단위 정책을 걸 수 없었다.
근무일지는 한 건이 한 행이라 가능하다.

```sql
-- 계정마다 어느 사업장 소속인지
create table if not exists worklog_member (
  email    text not null,
  site_id  text not null,
  primary key (email, site_id)
);
alter table worklog_member enable row level security;
create policy member_select on worklog_member for select to authenticated using (true);

-- 위 worklog_insert / worklog_update 정책을 지우고 아래로 갈아 끼운다
drop policy if exists worklog_insert on worklog;
drop policy if exists worklog_update on worklog;

create policy worklog_insert on worklog for insert to authenticated
  with check (exists (
    select 1 from worklog_member m
    where m.email = auth.jwt() ->> 'email' and m.site_id = worklog.site_id
  ));

create policy worklog_update on worklog for update to authenticated
  using (exists (
    select 1 from worklog_member m
    where m.email = auth.jwt() ->> 'email' and m.site_id = worklog.site_id
  ))
  with check (exists (
    select 1 from worklog_member m
    where m.email = auth.jwt() ->> 'email' and m.site_id = worklog.site_id
  ));
```

**이걸 켜기 전에 `worklog_member` 를 먼저 채운다.** 비어 있으면 아무도 일지를 못 쓴다.
읽기는 그대로 열어 둔다 — 인수인계 때문에 다른 초소 일지도 봐야 하는 경우가 많다.
읽기까지 막으려면 `worklog_select` 도 같은 방식으로 바꾼다.
