# 근무 일지

사업장(초소)마다 수기로 쓰던 근무일지를 디지털로 옮긴 내부용 도구.
지금은 **정문 초소 하나**로 시작하지만, 사업장·양식을 설정으로 정하는 구조라
초소나 대장을 늘릴 때 코드를 고치지 않아도 되게 만들었다.

## 배경 — 왜 만드는가

근무일지를 손으로 쓰면 **그 초소에 가야만 읽을 수 있다.** 인수인계 사항이나 특이사항이
다음 근무자에게 늦게 닿고, 월말에 모아 보려면 종이를 걷어야 한다.
쓰는 것과 나누는 것을 같이 쉽게 만드는 것이 이 도구의 목적이다.

**이 목적을 해치는 변경은 하지 않는다.** 쓰는 데 손이 더 가게 만들거나,
특이사항을 눈에 안 띄게 묻어 두거나, 다른 초소에서 못 보게 막는 방향은 안 된다.

**다만 이것은 「로그인한 직원」 에게 해당한다.**
주소만 알면 누구나 열 수 있는 화면이고 **근무일지에는 사람 이름이 들어 있으므로,**
로그인하지 않은 사람에게는 일지 본문을 아예 보여주지 않는다.

## 지금 어디까지 왔나

작업을 이어받을 때는 **`이관노트.md` 를 먼저 읽는다.** 시설 예약 대장에서 무엇을
가져왔고 무엇을 버렸는지, 어디를 먼저 고쳐야 하는지가 거기 있다.

**끝난 것**

- 달력·목록·설정 화면, 일지 작성/수정/삭제/복제, 같은 근무조 중복 안내
- **양식 엔진** — 일지에 어떤 칸이 있는지를 코드가 아니라 설정(`cfg.forms`)이 정한다.
  칸 일곱 종류(한 줄·여러 줄·숫자·시각·고르기·예/아니오·표)
- 특이사항·전달사항 표시 — 달력·목록·우측 상세에서 붉게/파랗게 세운다
- 사업장 구분색, 임시저장(파선), 날짜 확인 표시(✓)
- 공휴일 2040년까지 자동 판정 (표시용)
- 인쇄 두 가지 — 날짜별 일지 / 목록
- 엑셀·CSV 내보내기 (진짜 `.xlsx`. 같은 날짜 병합, 열 너비 자동, 요일 색)
- 로그인 · 역할(담당자/작성자/조회) · 되돌릴 수 없는 일 앞의 비밀번호 재확인

**아직 확인이 안 된 것 — 여기부터 이어서 한다**

1. **부서에서 쓰는 수기 양식을 받아 기본 양식(`FORM_GATE`)을 갈아 끼운다** — 지금 값은 추측이다
2. Supabase 표 생성 SQL 실행 (`서버설치안내.md` 2단계)
3. **RLS 켜기** (3단계) — 안 켜면 주소만 아는 사람이 근무일지를 읽는다
4. 신규 가입 차단 (4단계)
5. **두 사람이 동시에 써도 둘 다 남는지 확인** — 이 구조를 고른 이유다
6. **개인정보 검토** — `이관노트.md` 7번

## 사용자와 환경

- 부서 직원 내부용. 시민 대상 공개 서비스가 아니다
- **초소마다 근무자가 직접 쓴다.** 예약 대장과 달리 입력자가 여러 명이다
- 업무망/인터넷망 PC를 함께 쓴다. 브라우저는 Edge·Chrome 기준
- IT 지원이나 서버가 없다. 파일 하나로 돌아가야 한다

## 파일 구조

```
index.html        # 전부. HTML + CSS + JS 단일 파일, 빌드 없음
                  #   SUPABASE_URL / SUPABASE_ANON_KEY 를 섹션 1 위에 박아 넣는다
서버설치안내.md   # Supabase 표 · RLS · 계정 만들기
이관노트.md       # 시설 예약 대장에서 무엇을 가져왔나 · 먼저 할 일
README.md         # 저장소 첫 화면
.nojekyll         # Pages 의 Jekyll 처리를 끈다 (지우지 않는다)
```

**같은 화면의 복사본을 여러 파일로 두지 않는다.** `index.html` 하나만 고친다.

빌드 도구, 프레임워크, npm 의존성 **없음**. 브라우저에서 파일을 바로 열면 동작한다.
이 제약은 의도된 것이다 — 공유폴더에 파일 하나 두고 각자 여는 방식이 이 부서에서 유일하게
현실적인 배포 수단이다. **번들러·React·CDN 라이브러리를 도입하지 않는다**
(supabase-js SDK 도 포함 — REST 를 직접 fetch 한다).

외부 리소스는 Google Fonts 링크 하나뿐이고, 그것도 못 불러오면 시스템 폰트로 떨어진다.

## 코드 지도

`<script>` 안이 번호 붙은 섹션으로 나뉘어 있다. 주석 헤더로 찾으면 된다.

| 섹션 | 내용 | 자주 건드리는 함수 |
|---|---|---|
| 1 | 데이터 구조·저장 | `DEFAULT_CFG`, `FORM_GATE`, `migrateCfg`, `save`, `saveNow` |
| 1-B | **서버 연결 (행 단위)** | `logsLoad`, `logSave`, `logDelete`, `cfgLoad`, `serverPoll`, `sbGet` |
| 2 | 유틸·공휴일·역할 | `esc`, `ymd`, `toMin`, `dowClassOf`, `currentRole` |
| 3 | **양식 엔진** | `fieldHtml`, `readField`, `fieldText`, `flagTextOf`, `chipLabelOf` |
| 4 | 달력 | `calCellsHtml`, `renderCalendar`, `renderSummary` |
| 5 | 우측 상세 | `renderRail` |
| 6 | 목록 | `LIST_COLS`, `listCols`, `listRow`, `renderList` |
| 7 | 설정 화면 | `renderSettings`, `renderServerCard`, `openSettings` |
| 8 | 작성·수정 모달 | `openModal`, `formToLog`, `fillTimeSelects` |
| 9 | 인쇄 | `openPrintBox`, `buildPrintDetail`, `doPrint` |
| 9-C | 내보내기 | `exportTable` |
| 9-C-2 | xlsx 엔진 | `zipStore`, `xwNew`, `xwStyle`, `xwPut`, `xwXlsx` — **고칠 일 없음** |
| 10 | 렌더 총괄·권한·확인창 | `renderAll`, `applyRole`, `askConfirm` |
| 11 | 이벤트 | `boot`, `init` |

렌더는 전부 문자열 조합 후 `innerHTML` 이다.
**사용자 입력을 넣을 때는 반드시 `esc()` 를 거친다.**

## 양식 엔진 (섹션 3) — 이 도구의 중심

**일지에 어떤 칸이 있는지를 코드가 정하지 않는다.** `cfg.forms` 가 정한다.
`사업장 · 양식` 탭에서 칸을 더하면 작성창·목록·인쇄·엑셀이 모두 따라간다.

```js
{ key, label, type, required, hint, options, cols, flag, note }
```

| `type` | 무엇 | 값의 모양 |
|---|---|---|
| `text` | 한 줄 | 문자열 |
| `textarea` | 여러 줄 | 문자열 |
| `number` | 숫자 | 숫자 또는 null |
| `time` | 시각 | `"HH:MM"` — **24시간제 시·분 `<select>` 두 개다** |
| `select` | 고르기 | 문자열. 고를 값은 `options` 에 쉼표로 |
| `check` | 예/아니오 | true/false |
| `table` | 표(여러 줄) | `[[칸,칸,…], …]`. 열 이름은 `cols` 에 쉼표로 |

- `flag: true` 인 칸은 **특이사항**이다. 달력·목록·우측 상세에서 붉게 세운다.
  **이 표시를 빼지 않는다** — 다음 근무자가 여기부터 본다
- `note: true` 인 칸은 전달사항. 우측 상세에 파란 상자로 붙는다
- `required: true` 면 비우고 저장할 수 없다
- 표 형식은 **빈 줄을 저장할 때 버린다** (`readField`)

**새 칸 종류를 코드에 더하는 것은 정말 필요할 때만 한다.**
설정으로 될 일을 코드로 만들면 초소를 늘릴 때마다 코드를 고치게 된다.

**값을 사람이 읽는 글로 바꾸는 것은 `fieldText()` 하나가 한다** —
목록·인쇄·엑셀·CSV 가 모두 이것을 쓴다. 화면에서 본 것과 뽑은 종이가 달라지지 않게 하려는 것이다.

## 데이터 모델

**설정과 일지가 따로다.** 이것이 시설 예약 대장과 가장 크게 다른 점이다.

```js
// worklog_config 표의 id='main' 한 행
cfg = {
  version: 1,
  sites: [{ id, name, color, formId, shifts:[], startDefault, endDefault }],
  forms: [{ id, name, fields:[{ key, label, type, required, hint, options, cols, flag, note }] }],
  managers: [],    // 담당자 계정 이메일. **비어 있으면 로그인한 모두가 담당자다**
  doneDates: [],   // 확인을 마친 날 ("YYYY-MM-DD|사업장id")
  orgNote          // 인쇄물 머리글
}

// worklog 표 — **한 건이 한 행이다**
{
  id, site_id, log_date,      // "2026-08-14"
  shift,                      // "주간" | "야간" — 사업장의 shifts 에서 고른다
  start_at, end_at,           // "09:00"
  draft,                      // 임시저장
  data,                       // 양식 칸의 값 { 칸key: 값 }
  author, updated_at, updated_by
}
```

## 저장 방식 (섹션 1, 1-B)

| 모드 | 조건 | 동작 |
|---|---|---|
| server | `serverCfg()` 가 있음 | 서버에 저장. 15초마다 다른 사람 변경 확인 |
| local | 그 외 | localStorage |
| readonly | 로그인 안 함 / 권한 없음 | `body.ro` 를 붙여 쓰기 UI를 감춘다 |

**`body.ro` 는 서버 연결 카드(`#serverCard`)를 잠그지 않는다.** 여기까지 잠그면
로그인 입력란을 누를 수 없어 조회 전용에서 영영 빠져나오지 못한다. 이 예외를 없애지 않는다.

### ⭐ 일지는 한 건이 한 행이다

**여기가 시설 예약 대장과 결정적으로 다르다.**

예약 대장은 `ledger` 한 행에 대장 전체를 담는다 — 입력자가 한 명이라는 전제였다.
근무일지는 사업장마다 다른 사람이 동시에 쓰므로 그 구조를 쓰면
**한 사람이 저장하는 순간 그 사이 다른 사람이 쓴 일지가 통째로 사라진다.**

- `logSave(r)` 가 **그 한 건만** upsert 한다. **전체를 올리지 않는다**
- `logsLoad(y, m)` 가 **보이는 달만** 받아온다. 몇 해 치를 한꺼번에 들고 있지 않는다
- `serverPoll()` 은 그 달의 **가장 최근 `updated_at` 하나만** 받아본다 (수십 바이트).
  바뀌었을 때만 그 달 전체를 다시 받는다. **전체를 주기적으로 받지 않는다**
- 달을 옮기면 `reloadMonth()` 가 그 달을 새로 받는다

덤으로 **RLS 로 사업장별 쓰기 권한을 서버가 막을 수 있다** (`서버설치안내.md` 맨 아래).
예약 대장에서는 한 행 구조라 못 하던 것이다.

### 저장 시점

- `save()` → 마지막 변경 후 `AUTOSAVE_MS`(2분) 조용하면 자동 저장. **설정값에만 걸린다**
- `saveNow(silent)` → 저장 버튼 / `Ctrl+S` / `설정 저장`. 기다리지 않고 즉시.
  **이벤트 리스너에 직접 넘기지 않는다** (`() => saveNow()` 로 감싼다) —
  이벤트 객체가 `silent` 자리에 들어가 안내가 사라진다
- **일지 작성·수정·삭제는 `logSave`/`logDelete` 로 즉시 올린다.**
  달력에 뜬 일지가 다른 PC 에 안 보이는 시간이 없어야 한다
- **불러오기에 실패하면(`SERVER.on === false`) 저장하지 않는다.** 화면에 있는 옛 자료가
  서버의 최신 자료를 덮어쓰기 때문이다. 변경분은 `stashPending()` 으로 보관한다
- 저장 실패 시 변경분을 보관했다가 다음에 열 때 복구를 제안한다.
  **저장에 실패했다고 사용자 입력을 버리지 않는다**
- 지우는 동작은 **`clearPending()` 을 함께 부른다.** 안 부르면 지운 일지가 복구 안내로 되살아난다

### 서버 규칙 (예약 대장에서 그대로 가져온 것)

- **조회 요청은 `sbGet()` 으로 한다.** access token 은 한 시간쯤 지나면 만료되는데,
  그 토큰을 그대로 붙이면 401 이 와서 조회까지 막힌다. `sbGet()` 이 401 을 만나면
  갱신을 한 번 해보고, 그래도 안 되면 **토큰을 버리고 익명으로 다시 읽는다.**
  **`logsLoad`·`cfgLoad`·`serverPoll` 에서 `sbFetch` 를 직접 부르지 않는다**
- **주소는 프로젝트 기본 주소만 넣는다** (`https://xxxx.supabase.co`).
  `normalizeUrl()` 이 뒤에 붙은 `/rest/v1` 을 떼어낸다. **이 정규화를 빼지 않는다**
- **헤더 규칙에 주의.** publishable 키는 `Authorization: Bearer` 로 보내면 거부된다.
  `apikey` 헤더에만 싣고, `Authorization` 은 로그인한 사용자의 token 이 있을 때만 붙인다
- `SUPABASE_URL`/`SUPABASE_ANON_KEY` 를 코드에 넣어도 된다. **publishable key** 를 넣는다.
  **`sb_secret_` / `service_role` 키는 절대 코드에 넣지 않는다**

## 역할 — 담당자 / 작성자 / 조회

| 역할 | 조건 | 할 수 있는 것 |
|---|---|---|
| `manager` 담당자 | 로그인했고 `managers` 에 있음 (또는 목록이 비어 있음) | 전부 |
| `writer` 작성자 | 로그인했고 `managers` 에 없음 | 일지 작성·수정 |
| `viewer` | 로그인 안 함 | **일지 본문을 볼 수 없다** |

> ⚠️ **`managers` 목록이 비어 있으면 로그인한 모두가 담당자다.**
> 계정을 만들고 목록에 넣는 것을 빠뜨리면 그 계정이 전권을 갖는다.

### 로그인하지 않은 사람에게는 일지를 그리지 않는다

`body.viewer` 로 화면에서 빼는 데 그치지 않고, **본문을 만들어 넣지 않는다** —
감추기만 하면 개발자도구로 들여다볼 수 있고, 근무일지에는 사람 이름이 들어 있다.

| 어디 | viewer 에게 |
|---|---|
| 달력 칩 | **그리지 않는다** (`calCellsHtml` 의 `hideBody`). 건수만 남긴다 |
| 우측 상세 | **그리지 않는다** (`renderRail` 첫 줄). 로그인 안내만 |
| 목록 | **그리지 않는다** (`renderList` 첫 줄) |
| `사업장 · 양식` 탭 | 탭이 없고, 코드로 열어도 달력으로 되돌아간다 (`setPane`) |
| `+ 일지 작성`·`저장`·`확인`·인쇄·내보내기 | 화면에서 뺀다 |

- `applyRole()` 이 `body.no-admin` · `body.viewer` 를 붙여 고치는 UI 를 감춘다.
  **로그인 칸은 잠그지 않는다** — 잠그면 조회 전용에서 영영 빠져나오지 못한다
- 화면 잠금과 별개로 `canEditCfg()` 가 아니면 **설정 입력 처리 자체를 막는다** —
  잠긴 칸을 개발자도구로 건드려도 값이 바뀌지 않는다
- **관리자 전용 칸은 감추는 게 아니라 화면에 넣지 않는다.** `<template id="adminTpl">`
  에 넣어 두고 담당자일 때만 꺼내 붙인다. `hidden` 으로만 두면 구성원 이메일이 소스에 남는다.
  그래서 그 안의 단추에는 리스너를 직접 붙이지 않고 **`#settingsBox` 에 한 번만 위임한다**

**이 구분은 화면에서 가르는 것이고, 실제로는 서버의 RLS 가 막는다.**
예약 대장과 달리 근무일지는 한 건이 한 행이라 **진짜 권한 분리가 된다** —
그러니 화면 규칙만 믿지 말고 **RLS 를 반드시 켠다** (`서버설치안내.md` 3단계).

## 개인정보 ⚠️

**시설 예약 대장과 결정적으로 다르다.** 예약 대장은 연락처 항목을 의도적으로 제거해
개인정보를 담지 않는 구조로 만들었지만, 근무일지는 그 전략을 쓸 수 없다 —
근무자 성명·근무시간이 들어가고, 정문 초소 일지에는 방문자 성명·차량번호가 들어간다.

**즉 이 시스템은 개인정보를 처리한다.**
수집 근거·보유기간·안전조치·처리방침·위탁(Supabase) 검토가 필요하다.
**아직 확인되지 않았다** — `이관노트.md` 7번의 목록을 기관 개인정보 보호 담당자에게 확인한다.

개인정보 관련 판단이 필요하면 **`pipa-law` 스킬로 현행 조문을 조회한다.**
개정이 잦은 법이라 **기억에 의존해 답하지 않는다.**

## 디자인 규칙

- **색을 적게 쓴다.** 바탕은 흰색·검정 계열 회색조, 강조색 하나(`--accent`),
  경고·특이사항에만 `--alert`. 여기에 **사업장 구분색**과 **요일색**이 더해진다
- **사업장 구분색** — `--sc-{blue|orange|green|purple|none}-{soft|line|ink}` 세 벌로 정의하고,
  요소에는 `data-sc="blue"` 처럼 붙인다. 실제 색은 사업장의 `color` 값이며
  **설정 화면에서 고른다 — 이름으로 하드코딩하지 않는다**
- **요일색** — `--sat` 토요일 파랑 · `--sun` 일요일·공휴일 빨강.
  **글씨에만 쓴다.** 칸 전체를 물들이지 않는다 (칸 색은 사업장을 뜻하기 때문이다).
  **공휴일이 토요일이면 빨강이 이긴다**
- **이 용도 밖으로 색을 늘리지 않는다.** 새 색이 필요하면 팔레트에 한 벌을 더하고
  **세 테마 블록(`:root`, 미디어쿼리, `[data-theme="dark"]`)에 모두** 정의한다
- **달력 칸 바탕은 전부 흰색이다.** 초소는 쉬는 날이 없어서 주말을 회색으로 깔지 않는다.
  이번 달이 아닌 앞뒤 달 날짜(`.cell.out`)만 회색이다. **주말 배경을 되살리지 않는다**
- 특이사항은 `--alert` 상자, 전달사항은 `--accent` 상자. 뜻이 겹치지 않게 다른 색을 쓴다
- 임시저장은 **파선 테두리**로만 구분한다. 바탕색을 깔지 않는다
- 폰트: 본문 IBM Plex Sans KR, 숫자·시각은 IBM Plex Mono (`.mono`, 자릿수 정렬)
- **시각 입력은 24시간제 시·분 `<select>` 두 개다.** `<input type="time">` 은 브라우저
  지역 설정에 따라 오전/오후로 나오는데 코드로 막을 수 없다.
  **`type="time"` 으로 되돌리지 않는다.** 값은 `"HH:MM"` 이고 분은 10분 단위다
- **작성창은 바깥을 눌렀다고 닫히지 않는다.** 글자를 끌어 고르다 손을 창 밖에서 떼면
  그것도 바깥 클릭으로 잡혀 적던 내용이 통째로 날아간다.
  닫기는 `닫기 ✕`·`취소`·`Esc` 로만 한다 — **바깥 클릭 닫기를 되살리지 않는다**

## 내보내기 (섹션 9-C)

- **엑셀은 진짜 `.xlsx`(OOXML) 다.** HTML 표를 `.xls` 이름으로 넘기면 엑셀이
  **"파일 형식과 확장명이 일치하지 않습니다"** 경고를 띄운다. **HTML 표로 되돌리지 않는다**
- 같은 날짜의 날짜·요일 칸을 병합하고, 열 너비를 그 열에서 가장 긴 값에 맞춘다
  (한글은 두 칸으로 센다 · `textWidth`)
- **완료 칸은 내보내지 않는다.** 화면·종이에서만 쓰는 표시다
- 요일 글씨는 엑셀 표준 색 — 토 `0070C0`, 일·공휴일 `FF0000`. 특이사항은 붉은 굵은 글씨
- 줄 바탕 — 임시저장 `DEEBF7`(연한 파랑), 토·일·공휴일 `FFF2CC`(연한 금색).
  **임시저장이 요일 색을 이긴다.** **엑셀만 칠한다** — 화면·인쇄는 테두리와 글자색으로 구분한다
- **병합된 날짜 칸의 이어지는 줄에도 서식은 넣는다.** 비워 두면 날짜 묶음 굵은 줄이 끊긴다
- 용지는 가로 · 한 폭에 맞춤 (`wb.page`). **`wb.page` 없이 새 통합문서를 만들지 않는다**
- **CSV** 는 글자만 담는 형식이라 색도 병합도 담을 수 없다. 같은 날짜의 날짜·요일을
  비우는 것까지만 한다

## 변경 후 확인할 것

자동화된 테스트가 없다. 브라우저에서 파일을 열고 직접 본다.

**기본**

- [ ] 콘솔에 에러가 없다 (Google Fonts 연결 실패는 무시)
- [ ] 달력이 그려지고, 날짜를 누르면 오른쪽에 그 날 일지가 나온다
- [ ] 일지 작성 → 달력에 칩이 뜨고 → 새로고침해도 남아 있다
- [ ] 밝게/어둡게 두 테마에서 글자가 다 읽힌다 (우측 상단 ◐)
- [ ] `⚙ 설정` 맨 아래에 판 번호(`APP_VERSION`)가 보인다
- [ ] 왼쪽 위 제목을 누르면 첫 화면으로 새로고침된다

**작성창**

- [ ] 바깥을 눌러도, 글자를 끌어 고르다 밖에서 손을 떼도 창이 닫히지 않는다
- [ ] `Esc`·`취소`·`닫기 ✕` 로만 닫힌다
- [ ] 시각이 `09 시 00 분` 처럼 24시간제로 나온다 (AM/PM 아님)
- [ ] 표 형식 칸에 `+ 줄 추가` 로 줄이 늘고, `✕` 로 줄어든다
- [ ] 빈 줄만 있는 표는 저장하면 빈 값이 된다
- [ ] 필수 칸을 비우면 저장이 막히고 어느 칸인지 알려준다
- [ ] 같은 날 같은 근무조가 이미 있으면 안내가 뜨고, 한 번 더 누르면 저장된다

**양식**

- [ ] `사업장 · 양식` 탭에서 칸을 더하면 **작성창·목록·엑셀에 모두 나타난다**
- [ ] 칸 종류를 `표` 로 바꾸면 열 이름 입력칸이 나온다
- [ ] `특이사항` 을 켠 칸의 값이 달력·목록·우측 상세에서 붉게 나온다
- [ ] `↑`·`↓` 로 칸 차례가 바뀌고 목록 열 차례도 따라간다
- [ ] 사업장을 추가하면 달력 범례·고르기 목록에 함께 늘어난다

**권한**

- [ ] 로그인하지 않으면 달력에 칩이 없고 건수만 보인다
- [ ] 로그인하지 않으면 우측 상세·목록에 안내만 뜬다
- [ ] **로그인하지 않은 화면을 개발자도구로 봐도 근무자 이름이 소스에 없다**
- [ ] 로그인하면 위의 것이 모두 다시 보인다
- [ ] 담당자가 아니면 `사업장 · 양식` 이 잠기고 `설정 저장` 단추가 없다
- [ ] 로그인하지 않아도 로그인 칸은 쓸 수 있다
- [ ] 일지 삭제·이 달 전부 지우기를 누르면 **비밀번호를 다시 묻는다**
- [ ] 비밀번호를 틀리게 넣으면 아무것도 지워지지 않고 **로그인도 풀리지 않는다**

**서버**

- [ ] 주소·키를 입력하지 않은 새 브라우저에서도 서버에 연결된다
- [ ] 로그인 안 하면 조회 전용, 로그인하면 저장된다
- [ ] **두 창에서 각각 로그인해 서로 다른 일지를 쓰면 둘 다 남는다** ⭐
- [ ] 세션이 만료된 PC 에서도 화면이 뜬다 (조회 전용으로 내려갈 뿐)
- [ ] 불러오기에 실패하면 저장이 막히고 그 이유가 화면에 뜬다
- [ ] 다른 창에서 저장하면 15초 안에 이쪽 화면이 갱신된다
- [ ] 달을 옮기면 그 달 일지를 새로 받아온다

**인쇄 · 내보내기**

- [ ] `인쇄…` → `날짜별 일지` 에서 고른 날짜만, 표 형식 칸까지 찍힌다
- [ ] `목록` 인쇄는 지금 거르기 조건 그대로 나온다
- [ ] `이 달 엑셀` 파일 이름이 `.xlsx` 이고 **확장명 경고가 뜨지 않는다**
- [ ] 엑셀에 완료 칸이 없다
- [ ] 엑셀에서 같은 날짜의 날짜·요일이 합쳐져 있고, 날짜가 바뀌는 자리에 굵은 줄이 있다
- [ ] 요일 글씨가 토=파랑, 일·공휴일=빨강이다
- [ ] 임시저장 줄이 연한 파랑, 토·일·공휴일 줄이 연한 금색이다
- [ ] `이 달 CSV` 는 같은 날짜의 날짜·요일이 비어 있다

## 하지 말 것

- 프레임워크·번들러·CDN 라이브러리 도입 (단일 파일 제약. supabase-js SDK 포함)
- **일지를 통째로 올리기** (`logSave` 로 한 건씩 올린다. 통째로 올리면 남의 일지가 지워진다)
- 서버 자료를 주기적으로 통째로 내려받기 (`updated_at` 하나만 확인할 것)
- 몇 해 치 일지를 한꺼번에 들고 있기 (`logsLoad` 는 보이는 달만 받는다)
- 조회 요청에서 401 을 그냥 실패로 처리하기 (토큰을 버리고 익명으로 다시 읽어야 한다)
- 서버에서 불러오지 못한 상태로 저장하기 (남의 최신 자료를 덮어쓴다)
- **RLS 를 켜지 않고 배포하기** (근무일지에는 사람 이름이 들어 있다)
- `sb_secret_` / `service_role` 키를 코드나 저장소에 넣기 (대화창에 붙여넣는 것도 안 된다)
- 공개용 키를 `Authorization: Bearer` 헤더로 보내기 (새 키에서 거부된다)
- **로그인하지 않은 사람 화면에 일지 본문을 `hidden` 으로만 감추기** (개발자도구로 보인다.
  아예 그리지 않는다 — `calCellsHtml`·`renderRail`·`renderList`)
- 관리자 전용 칸을 `hidden` 으로만 감추기 (구성원 이메일이 화면 소스에 남는다)
- `esc()` 없이 사용자 입력을 `innerHTML` 에 넣기
- **양식 칸을 코드에 박아 넣기** (`cfg.forms` 로 정한다. 그래야 초소를 늘려도 코드를 안 고친다)
- 값을 글로 바꾸는 코드를 `fieldText()` 밖에 따로 두기 (화면과 종이가 어긋난다)
- 목록 열을 `listCols()` 밖에서 따로 만들기 (화면과 인쇄·엑셀이 어긋난다)
- 달력 칸 만드는 코드를 인쇄용으로 따로 두기 (`calCellsHtml()` 하나만 쓴다)
- 시각 입력을 `<input type="time">` 으로 되돌리기 (AM/PM 으로 나온다)
- **작성창을 바깥 클릭으로 닫기** (적던 내용이 날아간다)
- **달력 칸에 주말 배경 되살리기** (초소는 쉬는 날이 없어서 전부 흰색이다)
- 요일 색을 칸 배경에 쓰기 (글씨에만 쓴다)
- 사업장·요일 밖의 색상 추가
- 미디어쿼리나 `[data-theme]` 블록 **안에서만** 정의되는 색 만들기 (한쪽 테마에서 사라진다)
- 지우기·초기화를 `confirm()` 하나로만 막기 (`askConfirm()` 을 거친다)
- `verifyPassword()` 가 `SERVER.token` 을 갈아끼우게 만들기 (틀린 비밀번호에 로그인이 풀린다)
- 지우는 동작에서 `clearPending()` 빼먹기 (지운 일지가 복구 안내로 되살아난다)
- `saveNow` 를 이벤트 리스너에 직접 넘기기 (`() => saveNow()` 로 감싼다)
- **판 번호(`APP_VERSION`) 올리는 것을 빼먹기** — 고쳤는데 안 바뀐 것처럼 보인다.
  조회하는 사람도 볼 수 있게 관리자 전용 칸 바깥에 둔다
- 개인정보 관련 판단을 **기억에 의존해 답하기** (`pipa-law` 스킬로 현행 조문을 조회한다)
