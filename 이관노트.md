# 이관 노트 — 시설 예약 대장에서 근무일지로

새 저장소에서 작업을 이어받을 때 **이 문서부터 읽는다.**
무엇을 가져왔고, 무엇을 일부러 버렸고, 어디를 먼저 고쳐야 하는지를 적었다.

---

## 이 폴더에 무엇이 들어 있나

| 파일 | 무엇 |
|---|---|
| `index.html` | 전부. 2,900줄 남짓. **지금 브라우저에서 열면 바로 동작한다** |
| `서버설치안내.md` | Supabase 표 만들기 · RLS · 계정 만들기 |
| `CLAUDE.md` | 새 저장소용 작업 규칙 (예약 대장 것을 근무일지에 맞게 고쳐 썼다) |
| `README.md` | 저장소 첫 화면 |
| `.nojekyll` | GitHub Pages 의 Jekyll 처리를 끈다 (지우지 않는다) |
| `이관노트.md` | 이 문서 |

`index.html` 은 **서버 없이도 동작한다** — 주소·키를 안 넣으면 localStorage 에 저장한다.
먼저 열어서 손에 익힌 뒤 서버를 붙이는 순서를 권한다.

---

## 1. 그대로 가져온 것

예약 대장에서 이미 다듬어 놓은 것이라 다시 짤 이유가 없다.

| 무엇 | 예약 대장 위치 | 근무일지 위치 |
|---|---|---|
| **서버 연결·로그인·토큰 만료 복구** (`sbFetch`·`sbGet`·`serverLogin`·`normalizeUrl`) | 1-B (1890~2123) | 1-B |
| 저장 타이머·실패 시 보관(`stashPending`)·복구 제안 | 1 | 1 |
| 유틸 (`ymd`·`toMin`·`esc`·`textWidth`) | 2 | 2 |
| 공휴일 표 (2040년까지) | 1·2 | 2 |
| 달력 칸 만들기 (`calCellsHtml` 하나로 화면·인쇄 공용) | 5 | 4 |
| 24시간제 시·분 `<select>` (`fillTimeSelects`) | 9 | 8 |
| 되돌릴 수 없는 일 앞의 비밀번호 재확인 (`askConfirm`·`verifyPassword`) | 10 | 10 |
| **엑셀(.xlsx) 만들기** (`zipStore`·`xwNew`·`xwStyle`·`xwPut`·`xwXlsx`) | 9-D (3865~4152) | 9-C-2 — **한 줄도 안 고치고 통째로 복사했다** |
| CSS 변수·밝게/어둡게 두 테마·모달·인쇄 틀 | `<style>` | `<style>` |
| 역할 구분·관리자 전용 칸을 `<template>` 에 두는 방식 | 8 | 7·10 |

특히 **토큰 만료 복구(`sbGet`)** 와 **주소 정규화(`normalizeUrl`)** 는 예약 대장에서
실제로 사고가 나서 붙인 것이다. 빼지 않는다.

---

## 2. 일부러 버린 것

| 무엇 | 왜 |
|---|---|
| 섹션 3 이용료 계산 (`calcFee`·`dayBands`·`feeLinesHtml`) 전부 | 조례 전용. 근무일지에 요금이 없다 |
| 섹션 9-D 행사 정산서 · `SETTLE_FORM` · 통제 도장 | 〃 |
| 섹션 7-B 요금 관리 · 입금 확인 · `mergeRemotePayments` | 〃 (아래 3번 참고) |
| 감면·부속시설·관람료·공과금 | 〃 |
| 「준비 건」 개념과 노란 테두리 | 근무일지에는 준비/행사 구분이 없다. 대신 **임시저장**(파선)을 넣었다 |
| 상태(신청·승인·취소) | 예약 대장에서도 쓰지 않기로 한 것이라 처음부터 넣지 않았다 |

---

## 3. ⚠️ 가장 크게 달라진 것 — 저장 구조

**여기만은 예약 대장을 베끼면 안 된다.**

예약 대장은 `ledger` 표의 **`id='main'` 한 행에 대장 전체**를 담는다.
"입력자가 한 명이라 병합할 일이 없다" 는 전제로 고른 구조다.

근무일지는 **사업장마다 다른 사람이 동시에 쓴다.** 같은 구조로 만들면
A초소 근무자가 저장하는 순간, 그 사이 B초소 근무자가 쓴 일지가 통째로 사라진다.
예약 대장이 `mergeRemotePayments()` 로 땜질하고 있는 문제인데,
근무일지에서는 땜질로 감당이 안 되는 규모로 터진다.

그래서 **일지는 한 건이 한 행**이다.

| | 예약 대장 | 근무일지 |
|---|---|---|
| 표 | `ledger` 하나 | `worklog` (일지) + `worklog_config` (사업장·양식) |
| 저장 단위 | `state` 전체 | 일지 한 건 (`logSave(r)`) |
| 저장 시점 | 예약은 즉시, 나머지는 2분 | 일지는 즉시, 설정은 2분 |
| 폴링 | `ledger.updated_at` 하나 | 보이는 달의 `max(updated_at)` 하나 |
| 화면에 들고 있는 것 | 전부 | **보이는 달만** (`reloadMonth`) |
| 남의 변경 충돌 | `mergeRemotePayments` 로 땜질 | 애초에 안 부딪힌다 |
| 서버 권한 분리 | 못 한다 (한 행이라 열 단위 정책 불가) | **된다** — `site_id` 로 RLS 를 건다 |

마지막 줄이 덤으로 얻는 것이다. `CLAUDE.md` 에 "보안 경계로 설명하지 않는다" 고
적어 둔 그 한계를, 새 시스템은 지지 않고 시작한다.

---

## 4. 새로 만든 것 — 양식(폼) 엔진 (섹션 3)

예약 대장에서 「이용료 계산」 이 있던 자리다. **플랫폼으로 키우려면 여기가 핵심이다.**

일지에 어떤 칸이 있는지를 **코드가 아니라 설정(`cfg.forms`)이 정한다.**
`사업장 · 양식` 탭에서 칸을 더하면 작성창·목록·인쇄·엑셀이 **모두 따라간다.**

칸 종류 일곱 가지 — `한 줄` `여러 줄` `숫자` `시각` `고르기` `예/아니오` `표(여러 줄)`.
`표` 는 출입 기록·순찰 기록처럼 줄이 여러 개인 칸이다. 열 이름을 쉼표로 적는다.

칸에 붙일 수 있는 표시:

| 표시 | 무엇 |
|---|---|
| `필수` | 비우면 저장이 막힌다 |
| `특이사항` | 달력·목록·우측 상세에서 **붉게 세워** 보여준다 |
| `전달사항` | 우측 상세에 파란 상자로 붙인다 |

**정문 초소 하나만 쓰더라도 이 구조로 두는 것이 낫다.**
두 번째 초소를 붙일 때 코드를 고칠 필요가 없어진다 — 그것이 "플랫폼" 의 실체다.

지금 들어 있는 기본 양식(`FORM_GATE`)은 **추측으로 채운 것이다.**
부서에서 쓰는 수기 양식을 받으면 갈아 끼운다 — 아래 6번 참고.

---

## 5. 코드 지도

`<script>` 안이 번호 붙은 섹션으로 나뉘어 있다. 주석 헤더로 찾는다.

| 섹션 | 내용 | 자주 건드리는 것 |
|---|---|---|
| 1 | 데이터 구조·저장 | `DEFAULT_CFG`, `FORM_GATE`, `migrateCfg`, `save`, `saveNow` |
| 1-B | **서버 연결 (행 단위)** | `logsLoad`, `logSave`, `logDelete`, `cfgLoad`, `serverPoll`, `sbGet` |
| 2 | 유틸·공휴일·역할 | `esc`, `dowClassOf`, `currentRole` |
| 3 | **양식 엔진** | `fieldHtml`, `readField`, `fieldText`, `flagTextOf` |
| 4 | 달력 | `calCellsHtml`, `renderCalendar`, `renderSummary` |
| 5 | 우측 상세 | `renderRail` |
| 6 | 목록 | `LIST_COLS`, `listCols`, `listRow`, `renderList` |
| 7 | 설정 화면 | `renderSettings`, `renderServerCard`, `openSettings` |
| 8 | 작성·수정 모달 | `openModal`, `formToLog`, `fillTimeSelects` |
| 9 | 인쇄 | `openPrintBox`, `buildPrintDetail`, `doPrint` |
| 9-C | 내보내기 (엑셀·CSV) | `exportTable` |
| 9-C-2 | xlsx 엔진 | **고칠 일 없음** (예약 대장에서 통째로 복사) |
| 10 | 렌더 총괄·권한·확인창 | `renderAll`, `applyRole`, `askConfirm` |
| 11 | 이벤트 | `boot`, `init` |

렌더는 전부 문자열 조합 후 `innerHTML` 이다.
**사용자 입력을 넣을 때는 반드시 `esc()` 를 거친다.**

---

## 6. 먼저 할 일 (차례대로)

### ① 수기 양식을 받아 `FORM_GATE` 를 갈아 끼운다 ⭐

지금 들어 있는 칸(근무자·인수인계·순찰 기록·출입 기록·시설물 점검·특이사항·기타)은
**정문 초소 일지가 대개 이렇더라 하는 추측이다.** 실제 서식과 다를 것이 거의 확실하다.

두 가지 방법이 있다.

- **화면에서** — `사업장 · 양식` 탭에서 칸을 더하고 지운다. 코드를 안 고쳐도 된다
- **코드에서** — `FORM_GATE.fields` 를 고친다. 새로 설치하는 곳의 기본값이 된다

수기 양식 사진이나 항목 목록을 주면 그대로 옮겨 넣을 수 있다.

### ② 사업장 이름·근무조를 맞춘다

`사업장 · 양식` 탭에서 「정문 초소」 를 실제 이름으로 고치고,
근무조(`주간, 야간`)와 기본 근무시간을 실제 교대에 맞춘다.

### ③ Supabase 표를 만들고 RLS 를 켠다

`서버설치안내.md` 2~5단계. **RLS 를 반드시 켠다** — 근무일지에는 사람 이름이 들어 있다.

### ④ 저장소를 만들고 Pages 를 켠다

새 저장소를 만들고 이 폴더 내용을 올린 뒤,
Settings → Pages → `Deploy from a branch` 로 브랜치를 고른다.
(예약 대장의 `배포안내.md` 절차와 같다.)

### ⑤ 개인정보 검토 ⚠️

아래 7번을 반드시 읽는다.

---

## 7. ⚠️ 개인정보 — 예약 대장과 결정적으로 다르다

시설 예약 대장은 **연락처 항목을 의도적으로 제거해서** 개인정보를 담지 않는 구조로 만들었다.
신청 단체명만 기록하면 개인정보에 해당하지 않아, 도구 전체가 개인정보 처리에서 벗어난다 —
`CLAUDE.md` 에 "성명·전화번호 입력란을 다시 추가하지 않는다" 고 적어 둔 이유다.

**근무일지는 그 전략을 쓸 수 없다.**

- 근무자 **성명**과 **근무시간**이 기록된다 (인사·근태 정보)
- 정문 초소 일지에는 방문자 **성명·차량번호**가 들어간다 (기본 양식의 「출입 기록」)

즉 **이 시스템은 개인정보를 처리한다.** 예약 대장에 적용하던 "개인정보를 넣지 않는다" 규칙을
그대로 가져오면 안 되고, 반대로 개인정보 처리에 필요한 절차를 갖춰야 한다.

### 확인해야 할 것

**이 부분은 법령 원문을 확인하지 못한 상태로 적었다.**
작업 환경에서 법제처 국가법령정보 접속이 막혀 있어 현행 조문을 조회할 수 없었다.
아래는 "확인해야 할 목록" 이지 결론이 아니다 — **기관 개인정보 보호 담당자에게 확인한다.**

- [ ] **수집 근거** — 직원 근태는 동의 없이 처리 가능한 경우가 있으나(법령 의무·계약 이행 등),
      **방문자 성명·차량번호는 근거가 달라질 수 있다.** 각각 따로 확인한다
- [ ] **보유기간** — 근무일지를 얼마나 보관하고 언제 파기하는지.
      기관 기록물 보존기간과 맞춰야 한다. 지금 코드에는 **자동 파기 기능이 없다**
- [ ] **개인정보처리방침** — 기관 방침에 이 시스템의 처리 항목을 반영해야 하는지
- [ ] **안전조치** — 접근권한 관리·접근통제·접속기록 보관.
      지금 갖춘 것은 아래 「이미 되어 있는 것」 뿐이다
- [ ] **출입 기록의 성격** — CCTV(영상정보처리기기)와는 규율이 다르다.
      수기 출입대장을 디지털로 옮기는 것이 처리 방법 변경에 해당하는지
- [ ] **위탁·국외이전** — Supabase 는 외부 서비스다. **리전이 서울이어도**
      운영 주체가 국외 사업자라 검토가 필요할 수 있다. 이 항목을 빠뜨리기 쉽다

> 확인 방법: 이 저장소에서 `pipa-law` 스킬로 현행 조문을 조회할 수 있다
> (법제처 접속이 되는 환경에서). 조문은 개정이 잦으므로 **기억에 의존해 답하지 않는다.**

### 이미 되어 있는 것

- 로그인하지 않으면 **일지 본문을 화면에 그리지 않는다** — 감추는 것이 아니라
  아예 만들지 않는다 (`renderRail`·`renderList`·`calCellsHtml` 첫 줄).
  개발자도구로 봐도 근무자 이름이 소스에 없다
- 서버가 RLS 로 막는다 — 로그인하지 않으면 `worklog` 를 읽지 못한다
  (`서버설치안내.md` 3단계를 실행해야 적용된다)
- 누가 언제 고쳤는지 `updated_by`·`updated_at` 에 남는다

### 아직 안 되어 있는 것

- **보유기간이 지난 일지의 자동 파기** — 지금은 손으로 지운다
- **접속기록(누가 언제 열람했는지) 보관** — 수정 기록만 남고 열람 기록은 없다
- 개인정보처리방침 링크

이 셋은 위 확인 결과에 따라 필요하면 붙인다.
**필요 여부를 확인하기 전에 미리 만들지 않는다** — 안 쓰는 기능이 되면 관리만 늘어난다.

---

## 8. 하지 말 것

예약 대장의 「하지 말 것」 중 근무일지에도 그대로 해당하는 것과, 새로 생긴 것.

- 프레임워크·번들러·CDN 라이브러리 도입 (supabase-js SDK 포함). **단일 파일 제약**
- **일지를 통째로 올리기** — 한 건씩 올린다(`logSave`). 통째로 올리면 남이 쓴 일지가 지워진다
- 서버 자료를 주기적으로 통째로 내려받기 (`updated_at` 하나만 확인한다)
- 조회 요청에서 401 을 그냥 실패로 처리하기 (토큰을 버리고 익명으로 다시 읽어야 한다)
- 서버에서 불러오지 못한 상태로 저장하기 (남의 최신 자료를 덮어쓴다)
- `sb_secret_` / `service_role` 키를 코드나 저장소에 넣기 (대화창에 붙여넣는 것도 안 된다)
- 공개용 키를 `Authorization: Bearer` 헤더로 보내기 (새 키에서 거부된다)
- **RLS 를 켜지 않고 배포하기** — 근무일지는 사람 이름이 들어 있어 예약 대장보다 중하다
- **로그인하지 않은 사람 화면에 일지 본문을 `hidden` 으로만 감추기** (개발자도구로 보인다)
- `esc()` 없이 사용자 입력을 `innerHTML` 에 넣기
- 양식 칸을 코드에 박아 넣기 (`cfg.forms` 로 정한다. 그래야 초소를 늘려도 코드를 안 고친다)
- 목록 열을 `listCols()` 밖에서 따로 만들기 (화면과 인쇄·엑셀이 어긋난다)
- 달력 칸 만드는 코드를 인쇄용으로 따로 두기 (`calCellsHtml()` 하나만 쓴다)
- 시각 입력을 `<input type="time">` 으로 되돌리기 (브라우저에 따라 AM/PM 으로 나온다)
- **작성창을 바깥 클릭으로 닫기** (적던 내용이 날아간다)
- 달력 칸에 주말 배경 되살리기 (초소는 쉬는 날이 없어서 전부 흰색이다)
- 지우기를 `confirm()` 하나로만 막기 (`askConfirm()` 을 거친다)
- **판 번호(`APP_VERSION`) 올리는 것을 빼먹기** — 고쳤는데 안 바뀐 것처럼 보인다

---

## 9. 지금 확인된 것 / 확인 안 된 것

### 확인했다 (브라우저에서 돌려봤다)

- [x] 콘솔에 에러가 없다 (Google Fonts 연결 실패는 무시)
- [x] 달력 42칸이 그려지고, 날짜를 누르면 우측에 그 날 일지가 나온다
- [x] 일지 작성 → 달력에 칩이 뜬다. 양식 칸 7개가 그려진다
- [x] `표` 형식 칸에 줄이 추가·삭제된다. 빈 줄은 저장할 때 빠진다
- [x] 특이사항을 적으면 우측 상세에 붉은 상자로 뜬다
- [x] 목록에 고정 열 6개 + 양식 칸 7개 = 13열이 나온다
- [x] **로그인하지 않으면 근무자 이름이 화면 소스에 없다** (개발자도구로 확인)
- [x] 엑셀 파일이 진짜 `.xlsx` 다 (`file` 이 "Microsoft Excel 2007+" 로 읽는다)
- [x] CSV 에서 같은 날짜의 날짜·요일이 비워진다
- [x] `사업장 · 양식` 탭에서 사업장·칸이 그려지고 고쳐진다

### 확인 못 했다 (서버가 없어서)

- [ ] Supabase 에 실제로 연결되는지 — `서버설치안내.md` 7단계로 확인한다
- [ ] **두 사람이 동시에 써도 둘 다 남는지** — 이 구조를 고른 이유이므로 꼭 확인한다
- [ ] RLS 가 익명 조회를 실제로 막는지
- [ ] 인쇄 미리보기 모양 (브라우저에서 직접 본다)
- [ ] 엑셀을 실제 엑셀로 열었을 때 경고가 안 뜨는지

---

## 10. 나중에 「플랫폼」 으로 키울 때

지금 구조가 이미 그 방향으로 잡혀 있다. 다음 단계는 이런 순서가 자연스럽다.

1. **초소를 하나 더 붙인다** — `사업장 · 양식` 탭에서 추가. 코드를 안 고친다
2. **양식을 사업장마다 다르게 준다** — 이미 된다 (`site.formId`)
3. **사업장별 쓰기 권한을 서버가 막게 한다** — `서버설치안내.md` 맨 아래 `worklog_member`
4. **일지 말고 다른 대장을 붙인다** — 여기서 갈린다.
   양식 엔진(섹션 3)이 이미 "칸을 설정으로 정한다" 구조라,
   `worklog` 표에 `kind` 열을 하나 더해 여러 대장을 한 표에 담는 방법이 있다.
   다만 **대장마다 성격이 크게 다르면 표를 나누는 편이 낫다** —
   한 표에 억지로 담으면 예약 대장이 한 행에 전부 담았던 것과 같은 문제가 생긴다
5. **첫 화면에 대장 고르기를 둔다** — 그때 비로소 "여러 페이지를 만드는 플랫폼" 이 된다

4번이 갈림길이다. 그때 다시 상의하는 것을 권한다.
